import React from 'react'

function Rpapproveltable({darkMode,toggleDarkMode}) {
  return (
    <div  className={`main-body ${darkMode ? "dark-mode" : ""}`}>
      <div className="scrollonly-em">
        
      </div>
    </div>
  )
}

export default Rpapproveltable